## R CMD check results

`R CMD check --as-cran` was run locally with R 4.4.2 on macOS 26.3
(Apple silicon):

0 errors | 0 warnings | 3 notes

* This is a new submission.
* The note about verifying the current time is caused by the local check
  environment being unable to access the network; it does not indicate a
  future file timestamp in the package.
* The HTML validation note is a false positive from macOS's bundled 2006 HTML
  Tidy, which does not support the HTML5 `<main>` element generated by R.
  Current R detects that validator as obsolete and does not use it.

## Test environments

* Local macOS 26.3 (Apple silicon), R 4.4.2:
  `R CMD check --as-cran`.
* Local macOS 26.3 (Apple silicon), R 4.5.1:
  `R CMD build`, followed by `R CMD check --as-cran --no-manual` on the
  resulting source tarball, with the project `.Rprofile` disabled. The source
  build, vignette build, installation, documentation checks, examples, and
  testthat tests all passed. This run reported two expected NOTES because the
  check environment could not reach CRAN, GitHub, or a time service.
* Continuous integration is configured to check the package on macOS, Windows,
  and Ubuntu using R release, R-devel, and R oldrel-1.
